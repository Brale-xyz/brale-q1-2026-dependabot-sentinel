# Demo target dependencies for Dependabot Sentinel
# These are intentionally pinned to slightly older versions
# so dependabot will open PRs that the Sentinel workflow can review.

requests==2.32.5
httpx==0.25.2
pydantic==2.5.3
certifi==2024.2.2
urllib3==2.1.0

# charset-normalizer: pure encoding-detection utility, CalVer-free, clean releases.
# Pinned at 3.3.2 so dependabot proposes a 3.x minor bump — good auto-approve test case.
charset-normalizer==3.3.2
