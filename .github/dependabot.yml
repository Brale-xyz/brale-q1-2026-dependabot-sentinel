version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "daily"
    # Only open one PR at a time so the Sentinel workflow isn't flooded
    open-pull-requests-limit: 3
    commit-message:
      prefix: "deps"
